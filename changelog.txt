V3.0.32
     Ui fixes

V3.0.30
     keyword definitions clarified - fixed usage

V3.028
     added script info and multiple ui updates

V3.0.24
     UI Fix to give user confirmation that a symlink was followed for a script.

V3.0.23
     Added function to check help output to identify binaries that still show help with non zero exit codes,  
     Then fall back to man and info...

V3.0.21
     Fixed problem with variable expansion - Passed Shellcheck

V3.0.20
     Major overhaul of sourced file detection to handle more nested sourced files, conditional sourcing, etc.
     also follows bash internal logic for startup files now.
     detects rhel fedora and adds profile.d/*sh for interactive non login shells

V3.0.14
     Fixed a bug in dependency check

V3.0.12
     code cleanup. -- passed shellcheck

V3.0.10
     code cleanup. ui bug fixes.
     added support for batcat highlighting

V3.0.8
     code cleanup. ui bug fixes. 
     PATH manipulation code consolidated, broken out to a function
     PATH restoration handled by new function and a trap on return

V3.0.2
     Switch highlighting to use a perl heredoc instead of unreliable sed.
     Enhanced <TAB> completion, and PATH detection.

V3.0.0 
     I decided to rewrite h so that i could get help from multiple sources.
     Using common flags like ' --help', -help, -h, or -?.
     For builtins and some keywords use 'help "command"'
     As a fallback, checks for both man pages and info pages then alerts the user if found. 
     For aliases, functions and scripts - handle displaying contents.

V2.0.0 
     was a small function using fc similar to this:

       h() {
          last_cmd=$(fc -ln -1 | awk '{print $1}')
          # Run the last command with --help appended
          eval "$last_cmd --help"
      }

V1.0.0 
     was a small alias: alias h='eval "$(history -p !! | awk '''{print $1}''')" --help'
